package test;


struct LinkedList {
    value: int,
    next: LinkedList
}

pub fn make_linked_list() -> LinkedList {
    let head = LinkedList { value: 0, next: null };
    return head;
}

pub fn test2() -> int {
    let x = 25;
    x *= 2567;
    x += 25;
    x -= 25;
    x /= 25;
    x %= 25;
    return x;
}


















